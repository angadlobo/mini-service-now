// Portal module registration
// Portal tables (portal_announcements, portal_quick_links, portal_themes)
// do not have number sequences, so no table registry entries are needed.

export function registerPortalModule(): void {
  // No table registry entries needed for portal tables
}
